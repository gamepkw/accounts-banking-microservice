#!/bin/bash

# Add all changes
git add .

# Commit with a message
git commit -m "commit"

# Push to the remote repo./sitory (assuming "origin" is your remote name and "master" is the branch)
git push -u origin master